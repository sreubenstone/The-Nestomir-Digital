import React, { FC } from "react";
import { Text } from "react-native";
import Stylesheet from "../Stylesheet";

const Logo: FC = () => {
  return <Text style={Stylesheet.textCardTitle}>protoKids App</Text>;
};

export default Logo;
